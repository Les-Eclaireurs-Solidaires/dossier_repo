name: CI Pipeline

on:
  push:
    branches: 
      - main
      - feature/authentication

jobs:
  backend-build-and-test:
    name: Backend CI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "npm"
          cache-dependency-path: "./backend/package-lock.json"

      - name: Install dependencies
        run: npm ci

      - name: Vérification et Compilation TypeScript
        run: npm run build

      - name: Lancement des tests unitaires
        run: npm run test -- --run

  frontend-build-and-test:
    name: Frontend CI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "npm"
          cache-dependency-path: "./frontend/package-lock.json"

      - name: Install dependencies
        run: npm ci

      - name: Compilation Angular (SSR)
        run: npm run build

      - name: Lancement des tests unitaires (Vitest)
        run: npm run test -- --run